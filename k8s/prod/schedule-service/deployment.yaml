apiVersion: apps/v1
kind: Deployment
metadata:
  name: schedule-service
  namespace: nll-scheduler
  labels:
    app: schedule-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: schedule-service
  template:
    metadata:
      labels:
        app: schedule-service
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        lifecycle:
          preStop:
            exec:
              command: ["su-exec", "postgres", "pg_ctl", "stop", "-D", "/var/lib/postgresql/data", "-m", "fast"]
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: schedule-service-config
              key: POSTGRES_DB
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: schedule-service-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: schedule-service-secret
              key: POSTGRES_PASSWORD
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "schedule_user", "-d", "schedule_db"]
          initialDelaySeconds: 5
          periodSeconds: 5
      - name: schedule-service
        image: ghcr.io/mak3r/nll-scheduler/schedule-service:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: schedule-service-config
              key: PORT
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: schedule-service-secret
              key: DATABASE_URL
        - name: TEAM_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: schedule-service-config
              key: TEAM_SERVICE_URL
        - name: FIELD_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: schedule-service-config
              key: FIELD_SERVICE_URL
        - name: SCHEDULER_ENGINE_URL
          valueFrom:
            secretKeyRef:
              name: schedule-service-secret
              key: SCHEDULER_ENGINE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: schedule-service-postgres-pvc
