apiVersion: v1
kind: Service
metadata:
  name: schedule-service
  namespace: nll-scheduler-dev
  labels:
    app: schedule-service
spec:
  selector:
    app: schedule-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
